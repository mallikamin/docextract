services:
  - type: web
    name: docextract
    runtime: docker
    dockerfilePath: ./docker/Dockerfile
    dockerCommand: ./start.sh
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    plan: free
